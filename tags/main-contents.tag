<main-contents>
  <div class="well" style="margin: 24px;" each={ data }>{ title }</div>
  <script type="es6">
    if ( opts.data ) {
      this.data = opts.data
    } else {

    }
  </script>
</main-contents>
